import React from 'react';
import {View} from 'react-native';

interface Props {}

const Home = ({}: Props) => {
  return (
    <View
      style={{
        flex: 1,
        alignItems: 'center',
        justifyContent: 'center',
      }}>

    </View>
  );
};

export default Home;
